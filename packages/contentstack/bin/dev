#!/usr/bin/env node
require('dotenv').config({ path: '../../.env', encoding: 'utf8', debug: true, override: false });
(async () => {
  const oclif = require('@oclif/core');
  await oclif.execute({ type: 'cjs', development: true, dir: __dirname });
})();
