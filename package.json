{
  "name": "root",
  "devDependencies": {
    "lerna": "^5.3.0"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "private": true,
  "scripts": {
    "clean:build": "rm -rf ./build"
  },
  "license": "MIT",
  "standard": {
    "env": "mocha",
    "ignore": [
      "**/node_modules/**",
      "packages/*/lib/**"
    ]
  }
}