{
  "name": "root",
  "dependencies": {
    "@oclif/plugin-update": "^1.3.10"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "private": true,
  "scripts": {
    "clean:build": "rm -rf ./build"
  },
  "license": "MIT",
  "standard": {
    "env": "mocha",
    "ignore": [
      "**/node_modules/**",
      "packages/*/lib/**"
    ]
  },
  "workspaces": [
    "packages/*"
  ]
}
