{
  "name": "root",
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "private": true,
  "scripts": {
    "clean:build": "rm -rf ./build"
  },
  "license": "MIT",
  "standard": {
    "env": "mocha",
    "ignore": [
      "**/node_modules/**",
      "packages/*/lib/**"
    ]
  },
  "workspaces": [
    "./packages/*"
  ]
}
