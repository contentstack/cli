# Skills – Contentstack CLI

Source of truth for detailed guidance. Read [AGENTS.md](../AGENTS.md) first (skill index and stack summary), then open the skill that matches your task.

Each folder contains `SKILL.md` with YAML frontmatter (`name`, `description`).
